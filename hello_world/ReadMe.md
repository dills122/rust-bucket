# Introduction Setting Up and First Run

to install run:
```bash
curl https://sh.rustup.rs -sSf | sh
```


To run your first file execute the commands below

```bash
rustc main.rs # compile the file
./main # run the program
```